CONFIG = {
"wifi_ssid": "your-wifi-ssid",
"wifi_password": "your-wifi-password",
# Two letter country code for wifi region
"wifi_country": "NL",
#Timezone is a numerical value w/o dst offset. DST hardware switch adds +1 to this value
"timezone": 1,
# IP or hostname for ntp server. Leave this as an empty string to use the default gw as an ntp server.
"ntp_server": "europe.pool.ntp.org",
# brightness values are 0 (low) - 7 (high)
"display_brightness": 7,
}
